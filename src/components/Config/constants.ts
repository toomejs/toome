export enum ThemeDepend {
    OS = 'os',
    TIME = 'time',
    MANUAL = 'manual',
}
export enum ThemeMode {
    LIGHT = 'light',
    DARK = 'dark',
}
export enum LayoutMode {
    TOP = 'top',
    SIDE = 'side',
    CONTENT = 'content',
    EMBED = 'embed',
}
export enum LayoutComponent {
    HEADER = 'header',
    SIDEBAR = 'sidebar',
    EMBED = 'embed',
}
