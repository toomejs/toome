import type { LayoutVarsConfig } from './types';

export const defaultVars: Required<LayoutVarsConfig> = {
    sidebarWidth: '200px',
    sidebarCollapseWidth: '64px',
    headerHeight: '48px',
    headerLightColor: '#fff',
};
